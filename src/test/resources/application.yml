eessibasis:
  url: http://example.com:8089
  username: srvPensjon
  password: Ash5SoxP

aktoer.v2:
  endpointurl: https://example.com/aktoerid/AktoerService/v2

credentials:
  srvpensjon:
    username: srvPensjon
    password: Ash5SoxP

app:
  name: DEVELOP
  version: 0.1

securitytokenservice:
  url: https://example.com/SecurityTokenServiceProvider/

security:
  basic:
    enabled: false

management:
  security:
    enabled: false

oidc:
   clientId: eessi-fagmodul-test
   clientSecret: secret
   redirectUri: /openid_connect_login
   scope: openid
   issuerHost: https://example.com/isso/oauth2
   authorizationUri: ${oidc.issuerHost}/authorize
   tokenUri: ${oidc.issuerHost}/access_token
   userInfoUri: ${oidc.issuerHost}/userinfo
   jwkSetUri: ${oidc.issuerHost}/connect/jwk_uri